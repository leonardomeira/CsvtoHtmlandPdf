<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tabelinha</title>
    </head>
    <body>
        <table style="margin-inline: auto; font-family: Calibri; color: red">
            <thead>
                <% header.forEach(column =>{ %>
                    <th><%= column %></th>
                <% }) %>
            </thead>
            <tbody>
                <% body.forEach(row => { %>
                    <tr>
                        <% row.forEach(element => { %>
                            <td><%= element %></td>
                        <% }) %>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        <p>Made with CHP v0.1 Alpha - <a href="https://github.com/leonardomeira/chp">Source</a></p>
    </body>
</html>